{
  // Scripts run through tsx, so this project only handles editor type checking.
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "rootDir": "..",
    "noEmit": true
  },
  // Include src so its Express request type additions are available to scripts.
  "include": ["../src", "."]
}
