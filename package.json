{
  "private": true,
  "scripts": {
    "dev": "concurrently -n web,server -c blue,green \"npm --prefix web run dev\" \"npm --prefix server run dev\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
